Bonjour {{ user.get_username }},

Ton abonnement prestataire sur {{ site_name }} a été mis à jour.

Ancien plan : {{ old_plan.name }} ({{ old_plan.code }})
Nouveau plan : {{ new_plan.name }} ({{ new_plan.code }})

Résumé rapide :

- Services actifs autorisés : {{ old_plan.max_active_services }} → {{ new_plan.max_active_services }}
- Services mis en avant : {{ old_plan.max_featured_services }} → {{ new_plan.max_featured_services }}
- Commandes urgentes / jour : {{ old_plan.max_urgent_orders_per_day }} → {{ new_plan.max_urgent_orders_per_day }}
- Créneaux horaires max : {{ old_plan.max_time_slots }} → {{ new_plan.max_time_slots }}

Si tu n’es pas à l’origine de ce changement, contacte immédiatement le support.

L’équipe {{ site_name }}.