route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'email-admin'

receivers:
  - name: 'email-admin'
    email_configs:
      - to: 'siddick369@gmail.com'
        from: 'monitor@campuhub.site'
        smarthost: 'smtp.gmail.com:587'
        auth_username: 'siddick369@gmail.com'
        auth_password: 'YOUR_APP_PASSWORD'
        require_tls: true

# Example Telegram configuration (commented out)
#  - name: 'telegram-bot'
#    telegram_configs:
#      - bot_token: 'YOUR_BOT_TOKEN'
#        chat_id: YOUR_CHAT_ID
